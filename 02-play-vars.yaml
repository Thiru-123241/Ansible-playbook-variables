- name: play level variables
  hosts: local
  connection: local
  vars:
    NAME: Manish
    COURSE: MBBS
    FEE: 5Lakhs
  tasks:
  - name: print student data
    ansible.builtin.debug:
      msg: "Hello {{ NAME }}, course: {{ COURSE }},  Fee: {{ FEE }}"
